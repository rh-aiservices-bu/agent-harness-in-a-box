apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: openshell-gw
  annotations:
    haproxy.router.openshift.io/enable-h2c: "true"
spec:
  port:
    targetPort: grpc
  to:
    kind: Service
    name: openshell
